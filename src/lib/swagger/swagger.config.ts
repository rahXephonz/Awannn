import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Awannn API',
  description:
    'API documentation for CRUD Methods, You need to login to access the API see at /auth/signin documentations',
  version: '1.0',
  tags: ['CRUD'],
};
